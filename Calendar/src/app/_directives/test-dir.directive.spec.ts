import { TestDirDirective } from './test-dir.directive';

describe('TestDirDirective', () => {
  it('should create an instance', () => {
    const directive = new TestDirDirective(null);
    expect(directive).toBeTruthy();
  });
});
