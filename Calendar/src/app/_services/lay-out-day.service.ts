import {DataService} from "./data.service";

export const layOutDayService = (datainput: string) => {

  // const service = new DataService();
  const ret = 'true ' + datainput;
  console.log(ret);
};
